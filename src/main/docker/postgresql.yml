version: '2'
services:
    biblio-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/biblio/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=biblio
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
